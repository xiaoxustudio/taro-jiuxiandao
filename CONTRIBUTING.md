# 报告 bug

在开`issue`的时候，请搜索是否存在重复问题  
issue必要时需要包含截图或视频
语义清晰即可

# 贡献

1. 首先克隆我们的项目到您本地
2. 修改代码
3. 提交到`main`分支
4. 等待审核pr后合并

# 贡献注意事项

- 修复bug时请确认是否完全修复，并引用修复的`issue`
- feat新功能时请确保它是否应该存在，是否合理等因素
- 变量尽量用小驼峰命名，且语义清晰明了，特殊情况下可用拼音（<-PS：页面同理）
- 提交的commit标题需要标识：update(change)、feat、fix等字段，建议在冒号后加入一个空格，如:fix: xxx
- 能用`components`下的组件就尽量用它，如果用的一个组件且重复代码高，考虑将其封装成组件放置在`components`下
